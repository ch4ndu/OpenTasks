package com.udnahc.opentasks.ui.theme

import androidx.compose.ui.graphics.Color

// Primary
val PrimaryBlue = Color(0xFF4772FA)

// Priority colors (shared across themes)
val PriorityHigh = Color(0xFFFF5F68)       // Red — Urgent & Important
val PriorityMedium = Color(0xFFFFB000)     // Amber — Not Urgent & Important
val PriorityLow = Color(0xFF4772FA)        // Blue — Urgent & Unimportant
val PriorityNone = Color(0xFF0CCE9C)       // Green — Not Urgent & Unimportant

// Accent
val AccentOrange = Color(0xFFFF5F68)

// Date text color
val DateOrange = Color(0xFFE13E39)

// Completed task text
val CompletedTextColor = Color(0xFF808080)

// Dark theme
val DarkBackground = Color(0xFF010101)
val DarkSurface = Color(0xFF1C1C1C)
val DarkSurfaceVariant = Color(0xFF2C2C2C)
val OnDarkBackground = Color(0xFFE0E0E0)
val OnDarkSurfaceVariant = Color(0xFF9E9E9E)

// Light theme
val LightBackground = Color(0xFFF5F5F5)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEEEEEE)
val OnLightBackground = Color(0xFF1A1A1A)
val OnLightSurfaceVariant = Color(0xFF757575)
