# OpenTasks Copilot Instructions

Use the root [`AGENTS.md`](../AGENTS.md) file as the canonical repository instruction router.

Load only the task-relevant files under `docs/ai/` instead of loading all project guidance by default.
